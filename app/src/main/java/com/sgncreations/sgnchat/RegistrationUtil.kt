package com.sgncreations.sgnchat

object RegistrationUtil {

    /**
     * the input is not valid if the username/password is empty or the username is taken
     */

    fun validateRegistrationInput(
        username: String,
        email: String,
        password: String,
    ): Boolean {
        return true
    }
}